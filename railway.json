{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "system/**",
      "index.php",
      "public/**"
    ],
    "buildCommand": "cd system && composer install --no-dev --optimize-autoloader && php artisan config:cache && php artisan route:cache"
  },
  "deploy": {
    "startCommand": "php -S 0.0.0.0:$PORT server.php"
  }
}
